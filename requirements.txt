# Install torch separately with the CUDA build that matches your driver:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Versions below follow Table 1 of the paper where it pins them.
numpy>=1.24
pandas>=1.5
scikit-learn>=1.3
matplotlib>=3.7
PyYAML>=6.0

# Optional: only needed for src/run_federated_flower.py and
# tools/verify_fedavg_equivalence.py. The main runner does not require it.
# flwr==1.7.0
